export default function LinksGateway({ onClick }) {
    return (
      <div className="App-body gateway G-links">
        <button className="button config G-button" onClick={onClick}>View Important Links</button>
      </div>
    )
  }